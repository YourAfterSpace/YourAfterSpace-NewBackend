## Development overrides
logging.level.root=INFO
logging.level.com.example.yas_backend=DEBUG

# Actuator endpoints are useful during development.
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always

# AWS Cognito Configuration for local development
# These values should be set via environment variables or .env file
# For local development, you can override here or use environment variables
# aws.cognito.user-pool-id=${AWS_COGNITO_USER_POOL_ID:your-dev-pool-id}
# aws.cognito.client-id=${AWS_COGNITO_CLIENT_ID:your-dev-client-id}
# aws.cognito.client-secret=${AWS_COGNITO_CLIENT_SECRET:}
# aws.region=${AWS_REGION:eu-west-2}
